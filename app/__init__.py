"""Application package for X List Summarizer.

The legacy UI can still be launched as ``python app/web_ui.py``.  Keeping the
package initializer dependency-free also makes the task orchestration modules
usable in tests and offline tooling without importing network SDKs.
"""

